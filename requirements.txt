# DeepSeek Visor Agent - Core Dependencies

# Core ML dependencies
torch>=2.6.0
torchvision>=0.21.0
transformers==4.46.3  # Fixed version - required for DeepSeek-OCR compatibility
tokenizers>=0.20.0,<0.21.0

# Model dependencies
einops
addict
easydict
matplotlib>=3.5.0  # 3.10+ requires Python 3.10+

# Image processing
pillow>=10.0.0

# PDF processing
PyMuPDF>=1.23.0  # PDF to image conversion, DeepSeek-OCR official recommendation

# Utilities
psutil>=5.9.0
pydantic>=2.0.0

# Logging
loguru>=0.7.0
